# 다운로드 이후 자동화 계획 및 구현 범위

## 목표

포털 로그인과 파일 다운로드는 담당자가 수행한다. 이후에는 받은 파일을 한 번 선택하고, 프로그램이 자료 분류·직원 연결·입력·계산·검산용 상태 갱신까지 수행한다. 최종 적용, 승인, 월 마감과 개인정보 파일 보관은 담당자의 명시적 확인을 남긴다.

## 구현 흐름

1. 현재 급여월을 연다.
2. 통합산출·원장 전달자료·보험 포털 XLSX/CSV를 한꺼번에 선택하거나 끌어놓는다.
3. 브라우저 안에서 XLSX 압축과 XML을 읽고 각 시트 헤더로 자료 종류를 찾는다. 서버 전송은 없다.
4. 성명과 별칭으로 후보를 찾되 실제 반영 대상은 직원 `id`로 고정한다.
5. 파일별·직원별 변경값, 연결 실패, 동명이인, 월 불일치와 값 충돌을 미리 보여준다.
6. 담당자가 적용하면 고정급여, 변동급여, 연차, 보험 원자료를 현재 월 데이터에 한 번에 반영한다.
7. `payroll.js`가 시간외·보험·세금·지급계·공제계·실지급액을 다시 계산한다.
8. 적용 시각, 파일명, 자료 종류, 적용·신규 직원 건수를 해당 월 가져오기 기록에 남긴다.

## 자동 인식 규칙

| 자료 | 식별 헤더 | 반영 내용 |
|---|---|---|
| 통합 급여표 | 교사, 기본급, 시간외, 지급 계 | 직원 급여 기준과 월 변동 지급·공제 |
| 시간외 | 별칭 열과 총 합 행 | 직원별 시간외 합계와 항목별 근거 |
| 연차 | 연차 사용 현황, 총연차, 잔여연차 | 연도별 부여와 누적 사용 |
| 제수당 | 교사, 근로지원금, 제수당 | 고정 근로지원금, 추가수당, 사협 지급 |
| 국민연금 | 가입자명, 결정보험료 | 결정액과 근로자 부담 자동계산 |
| 건강·요양 | 성명, 고지보험료 2열 | 건강보험·장기요양 고지액 |
| 고용보험 | 근로자명, 보험료합계 | 근로자 실업급여 보험료 합계 |
| 두루누리 | 근로자/성명, 지원금 | 국민연금·고용보험 근로자 지원액 |
| 표준 CSV | 성명과 2개 이상의 지원 헤더 | 새로 전달받은 급여·수당·보험 값 |

## 안전장치

- 현재 월과 파일의 감지 월이 다르면 적용 직전에 재확인한다.
- 마감 월에 적용하려면 기존 마감 해제 경고를 통과해야 한다.
- 동명이인은 자동 연결하지 않고 UUID 선택을 요구한다.
- 보험·제수당에만 등장하는 과거 직원은 자동 생성하지 않는다.
- 통합 급여표나 표준 입력표의 새 직원만 명시적 미리보기 상태에서 새 UUID로 추가한다.
- 같은 직원·필드 값이 파일마다 다르면 아래쪽 자료가 적용됨을 경고한다.
- 보험 종류를 가져올 때 그 파일에 없는 재직자의 해당 원자료는 0으로 초기화해 전월 값이 남지 않게 한다.
- 주민번호, 보험증번호, 연락처, 계정과 비밀번호는 어떤 가져오기 매핑에도 포함하지 않는다.
- 원본 파일 내용은 브라우저 메모리에서만 처리하고 `localStorage`에는 필요한 급여 필드와 가져오기 이력만 저장한다.

## 의도적으로 사람에게 남긴 단계

- 포털 로그인, 인증서·2차 인증과 자료 다운로드
- 파일 선택 및 적용 전 미리보기 확인
- 원장·재정이사 승인 체크
- 인쇄/PDF·CSV·JSON을 저장할 위치 선택
- 월 마감 및 백업 파일의 접근 제한 위치 이동

브라우저는 사용자 허가 없이 `원본자료/` 폴더를 감시할 수 없으므로 파일 선택까지 없애려면 별도의 로컬 서버나 설치형 프로그램이 필요하다. 현재 구현은 빌드 없는 정적 페이지와 개인정보 로컬 처리 원칙을 유지하면서 가능한 범위까지 자동화한다.
